@pubs.each_with_index do |pub, index|
  page << "var point = new GLatLng(#{pub.lat}, #{pub.lng});"
  page << "var marker = createMarker(point, #{pub.id}, #{index+1});"
  page << "map.addOverlay(marker);"  
end

page.replace_html 'map-pub-listing', :partial => 'map/map_pub_listing'
