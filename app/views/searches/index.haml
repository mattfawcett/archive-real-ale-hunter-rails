%h2="Search results for #{h params[:terms]}"

= will_paginate @results


- @results.each do |pub|
  = link_to pub.excerpts.name, pub_path(pub)
  = " - " + pub.address(excerpts=true)
  %br/
  = pub.excerpts.description
  %br/
  %br/
  
= will_paginate @results